# Update theme typing

## Why?

To extend the internal theme typings to provide nice autocomplete of added or updated theme components, component variations, sizes, colors and other theme foundations

## How?

Just run the following command after updating the theme:

```bash
pnpm theme:generate-typing
```
